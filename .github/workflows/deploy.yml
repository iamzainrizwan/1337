name: deploy 1337

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: deploy app
        run: |
          cd /opt/services/1337:
          git pull
          docker compose up -d --build
